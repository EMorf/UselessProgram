rootProject.name = 'UselessProgram'

// Enable local build cache for faster builds
buildCache {
    local {
        enabled = true
    }
}

// Enable parallel execution for faster builds
// This is best set in gradle.properties, not settings.gradle
